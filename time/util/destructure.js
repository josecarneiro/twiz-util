module.exports = date => {
  return (({
    milliseconds,
    seconds,
    minutes,
    hours,
    day,
    month,
    year
  }) => ({
    milliseconds,
    seconds,
    minutes,
    hours,
    day,
    month,
    year
  }))(date);
};
