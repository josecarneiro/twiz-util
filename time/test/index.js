describe('Utils Tests', () => {
  /* BEFORE AND AFTER TEST HOOKS */
  before(done => {
    done();
  });

  after(done => {
    done();
  });

  /* REQUIRE TESTS */
  require('./instant');
  require('./interval');
});
